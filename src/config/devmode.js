// DEV MODE — set to false before the actual day
export const DEV_MODE = true;

// When DEV_MODE is true, GPS check is bypassed and both players
// are considered "inside" the geofence immediately
export const DEV_PLAYER = null; // null means both players get dev mode
